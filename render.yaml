services:
  - type: web
    name: vue2-learning-app
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm install -g serve && serve -s dist
    staticPublishPath: dist
    envVars:
      - key: NODE_ENV
        value: production
